import { StyleSheet } from 'react-native'
import { THEME_COLORS } from '../constants/AppInfos'

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: THEME_COLORS.c1000,
    },
    textprimary: {
        color: THEME_COLORS.c600
    }
})